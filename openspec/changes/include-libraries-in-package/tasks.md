## 1. Package Configuration

- [ ] 1.1 Add `"libraries"` to the `"files"` array in `package.json`

## 2. Verification

- [ ] 2.1 Run local packing/validation to verify `libraries` directory exists in package payload
- [ ] 2.2 Run `openspec validate include-libraries-in-package --type change --strict`
